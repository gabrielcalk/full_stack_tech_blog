<main>
    <h2 class="mt-4 border border-dark rounded bg-primary p-2" id="dash">Your Dashboard</h2>
    {{#each posts_user}}
    <section class="w-75 mt-4 box_tech">
        <div class="d-flex justify-content-between align-items-center bg-dark">
            <h2 class="text-white px-3 pt-1">{{this.title}}</h2>
            <p class="text-white px-3 pt-2">Posted by {{this.user.username}} on {{date this.updated_at}}</p>
        </div>
        <p class="px-3 pt-2">{{this.description}}</p>
    </section>
    {{/each}}
    <section class="d-flex justify-content-center">
        <button class="p-2 px-3 mt-5 rounded">+ New Post</button>
    </section>
</main>